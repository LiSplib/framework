<div id="postsList" class="w-100 mb-3">
    <?php
    foreach ($data as $d) :
    ?>
        <div class="post">
            <?php
            if ($d->media_type === 'VIDEO') {
            ?>
                <video controls>
                    <source src="<?= $d->media_url ?>">
                </video>
            <?php
            } else {
            ?>
                <img src="<?= $d->media_url ?>">
            <?php
            }
            ?>
            <?php
            ?>
        </div>
    <?php
    endforeach; ?>
</div>
<div class="d-flex justify-content-center mb-5">
    <form action="<?= route_to_url('instagram') ?>?previous" method="GET">
        <button type="submit" id="preview" class="btn grey darken-1" name="previous">retour</button>
    </form>
    <form action="<?= route_to_url('instagram') ?>?next" method="GET">
        <button type="submit" id="next" class="btn grey darken-1" name="next">suivant</button>
    </form>
</div>